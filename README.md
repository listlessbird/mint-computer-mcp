# mint-computer-mcp

Computer-use MCP server for Linux Mint X11 desktops. wip.

## Development

Use Python 3.12, uv, and just. Install the locked dependencies and local Git hooks:

```sh
just sync
just hooks
```

Run the checks:

```sh
just check
```
